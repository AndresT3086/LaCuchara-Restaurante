# ============================================================
# .env.example — Copia este archivo como ".env" y rellena los valores
#   cp .env.example .env
#
# ⚠️  El archivo ".env" NUNCA se sube a GitHub
#     Verifica que .gitignore tenga la línea: .env
# ============================================================

# URL de conexión a Supabase
# Obtenerla en: Supabase → Settings → Database → Connection string → URI
DATABASE_URL=postgresql://postgres:LaCucharaRestoBar@db.bnfawqyixbeftyuzpzux.supabase.co:5432/postgres